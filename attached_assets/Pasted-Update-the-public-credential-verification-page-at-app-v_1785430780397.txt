Update the public credential verification page at:

`app/verify/[code]/page.tsx`

The goal is to make the page clean, simple, and focused on confirming whether a credential is valid.

For a valid `PUBLISHED` credential, show only the following sections in this order:

1. **Verification status**

   * Verified badge
   * Intern’s full name
   * Credential number
   * Credential status

2. **Certificate preview**

   * Display the certificate PNG prominently when `hasCertificatePreview` is true.
   * Preserve the certificate’s aspect ratio.
   * Make it responsive on mobile and desktop.
   * Hide the entire section when no preview exists.

3. **Document downloads**

   * Show **Download Certificate (PDF)** when `hasCertificatePdf` is true.
   * Link it to `/api/public/credentials/[token]/certificate?download=1`.
   * Show **Download Recommendation Letter (PDF)** when `hasLetterPdf` is true.
   * Link it to `/api/public/credentials/[token]/letter?download=1`.
   * Display both buttons side by side on desktop and stacked on mobile.
   * Remove the separate “View Letter in Browser” button.

4. **Skills demonstrated**

   * Keep the existing skills chips.
   * Hide this section completely when no skills are available.

Remove these separate sections from the verification page:

* Internship dates
* Position and department
* Issue date and issuer
* Projects and responsibilities
* Founder recommendation
* Manager recommendation

These details are already available in the certificate or recommendation letter and should not be repeated.

Preserve the existing revoked and not-found states. If a credential is revoked, missing, or not `PUBLISHED`, do not show the certificate preview, download buttons, or skills.

Technical requirements:

* Change only `app/verify/[code]/page.tsx`.
* Do not modify the database, APIs, document templates, or credential-generation logic.
* Preserve server-side credential retrieval.
* Preserve `noindex`, `no-store`, and existing cache protections.
* Use the existing design system and components.
* Do not add dependencies.
* Do not invent new fields, routes, or availability flags.
* Ensure the page is responsive and accessible.
* Run the existing type-check, lint, and relevant tests after implementation.
* Report what changed and disclose any checks that could not be completed.
